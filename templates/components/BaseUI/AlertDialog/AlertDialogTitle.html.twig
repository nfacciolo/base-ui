<{{ this.headingTag }}
    id="{{ this.id }}"
    {% for attr, value in this.dataAttributes %}
        {{ attr }}="{{ value }}"
    {% endfor %}
    {{ attributes }}
>
    {% block content %}{% endblock %}
</{{ this.headingTag }}>
